import { selected } from 'bicycle-rent/helpers/selected';
import { module, test } from 'qunit';

module('Unit | Helper | selected');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = selected([42]);
  assert.ok(result);
});
