import { dateToISOFormat } from 'bicycle-rent/helpers/date-to-iso-format';
import { module, test } from 'qunit';

module('Unit | Helper | date to iso format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateToISOFormat([42]);
  assert.ok(result);
});
